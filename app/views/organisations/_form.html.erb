<%= form_with(model: organisation) do |form| %>
  <% if organisation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organisation.errors.count, "error") %> prohibited this organisation from being saved:</h2>

      <ul>
        <% organisation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :hourly_rate %>
    <%= form.text_field :hourly_rate %>
  </div>
<div class ="field form-group">
  <%= form.number_field :user_id, id: :organisation_user_id, class:"form-control", value: current_user.id, type: :hidden %>
</div>
<%current_user.id %>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
